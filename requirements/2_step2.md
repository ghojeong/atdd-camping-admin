# 2단계 - 중복 제거와 구조화

## 요구사항

### 1. 예외/엣지 케이스 시나리오 확장

* Step 1에서 선택한 기능의 다양한 예외 및 엣지 케이스 시나리오를 추가하세요.

### 2. Step Definition 리팩터링

* 같은 기능 내에서 시나리오를 확장하면서 필연적으로 발생하는 중복 Step Definition들을 찾아냅니다.
* 반복 Step을 파라미터화하고, 공통 헬퍼로 추출합니다.

### 3. 테스트 인프라 분리

* 여러 기능에서 반복적으로 사용되는 준비 코드(JWT 토큰 발급, 테스트 데이터 생성 등)를 별도의 Helper 또는 Factory 클래스로 분리하세요.

## 제출 산출물

### 1. 추가 시나리오가 포함된 feature파일

```feature
# src/test/resources/features/selected-feature.feature

Feature: 예약 취소

Scenario: 사용자가 예약한 건을 관리자가 취소하면 성공하고 재예약 가능하다
...

Scenario: 취소된 예약은 취소가 불가능하다
...

# ... 그 외 다양한 시나리오 ...
```

### 2. 리팩터링 된 Step 클래스 혹은 재사용 가능한 형태의 클래스와 메서드

## 체크포인트

* [] 2개 이상의 예외/엣지 케이스 시나리오를 추가했는가?
* [] 중복되는 Step Definition을 파라미터화하여 하나로 통합했는가?
* [] JWT 토큰 생성 및 테스트 데이터 준비 로직을 별도 클래스로 분리했는가?
